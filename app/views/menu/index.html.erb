<h1>Saved Games</h1>

<% if @saved_games.present? %>
  <% @saved_games.each do |saved_game| %>
    <%= link_to saved_game.name, saved_game_path(saved_game) %><br>
  <% end %>
<% else %>
  <p>No saved games found.</p>
<% end %>

<%= link_to 'New Game', new_saved_game_path, class: 'btn btn-primary' %>